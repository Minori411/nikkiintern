
<h4>Top News</h2>
<div class="row">
  <article class="col-8">
  <% @news.each do |news| %>
    <%= link_to news_path(news.id) do %>
    <div class="row" style="margin:0;">
        <div class="col2 s12 m5">
            <!-- card start -->
            <div class="card" style="display:flex;height: 150px;width: 400px; margin:0;">
                <div class="card-image">
                <%=image_tag "cat.png", height: '150', width: '150%' %>
                <span class="card-title"><%= news.title %></span>
            </div>
            <div class="card-content">
            <%= news.created_at.strftime('%Y/%m/%d %H:%M') %>
            <% if current_user.userstyle == 0 %>
            <% end %>
            <div class="fromto">
            <h6>FROM Aエリアpipeline <br>TO Bエリアpipeline</h6>
            </div>
            </div>
            </div>
            <!-- card end -->
        </div>
        </div>
    <% end %>
  <% end %>
  </article>
  <aside class="col-4">
    <%= form_with url: search_path, method: :get, local: true do |f| %>
        <div class="row">
    <div class="col s12 m4" style="width:100%;width: 400px;height: 100%; height: 450px;">
      <div class="card-panel teal">
      <h2 class="search-title">絞り込み検索</h2>
      <%= f.check_box :a_processarea, {}, "a_processarea", "unchecked-value" %>
      <%= f.check_box :a_processarea, {}, "a_processarea", "unchecked-value" %>
      <%= f.check_box :a_processarea, {}, "a_processarea", "unchecked-value" %>
      <%= f.check_box :a_processarea, {}, "a_processarea", "unchecked-value" %>
      <%= f.check_box :a_processarea, {}, "a_processarea", "unchecked-value" %>
      </div>
    </div>
  </div>
        
    <% end %>
  </aside>
</div>
