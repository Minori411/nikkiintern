<% notifications = @notifications.where.not(user_id: current_user.userstyle == 0) %>
<% if notifications.exists? %>
  <%= render 'notifications' %>
  <%= paginate 'notifications' %>
<% else %>
  <p>
    | 通知はありません
  </p>
<% end %>